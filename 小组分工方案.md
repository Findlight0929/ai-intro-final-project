# 人工智能导论大作业三人分工方案

## 一、项目目标

本项目围绕“可解释的谣言检测”展开，目标是构建一个能够对输入文本进行谣言分类并给出判断依据的系统。

系统输入一段文本，输出：

1. 分类结果：
   - `0` 表示非谣言
   - `1` 表示谣言
2. 判断依据：
   - 用一段自然语言说明模型为什么做出该判断

本项目需要同时满足以下要求：

- 在 `val.csv` 上取得较好的分类准确率
- 模型可以实际运行，流程完整
- 仓库结构清晰，具备部署和运行说明
- 最终形成课程报告 `report.pdf`
- 仓库中能够体现三位成员的实际贡献

---

## 二、总体技术路线

本项目采用“分类模型 + 解释模块”的组合方案。

### 1. 分类模块
负责输出谣言分类结果，建议按以下顺序推进：

- 第一版 baseline：`TF-IDF + Logistic Regression`
- 第二版改进模型：`TF-IDF + SVM` 或 `Naive Bayes`
- 第三版增强模型：如有余力，尝试英文预训练语言模型微调

### 2. 解释模块
负责输出判断依据，建议分两层实现：

- 保底解释：基于关键词、文本特征、情绪表达、来源特征等生成规则化解释
- 增强解释：调用学校提供的大语言模型接口，将分类结果转化为更自然的解释文本

### 3. 系统集成模块
将分类模块与解释模块封装为统一接口，实现以下流程：

输入文本 → 输出分类标签与判断依据

最终至少支持以下一种运行方式：

```bash
python main.py --text "input text here"
```

---

## 三、数据集情况

当前数据集中已确认包含以下字段：

- `id`
- `text`
- `label`
- `event`

其中：

- `text` 为输入文本
- `label` 为分类标签
- `event` 表示所属事件

该数据集为英文社交媒体谣言检测数据，因此建模和预处理应按英文文本任务设计。

---

## 四、三人分工安排

### 成员 1：组长（项目统筹、系统集成、报告负责人）

#### 主要职责

- 建立和维护 GitHub 公共仓库
- 统一目录结构、代码规范和输入输出接口
- 整合分类模块与解释模块
- 编写 `README.md`
- 汇总实验结果并完成 `report.pdf`
- 检查最终提交材料完整性

#### 具体任务

1. 创建项目仓库并邀请组员协作
2. 设计并维护项目目录结构
3. 规定统一输出格式，例如：
   - `label`
   - `reason`
4. 整合训练、预测、解释脚本为统一入口
5. 运行最终实验并汇总结果
6. 完成部署说明、运行说明和项目说明
7. 统筹课程报告写作和最终提交

#### 预期产出

- 完整项目目录结构
- `README.md`
- `main.py` 或统一推理脚本
- 最终实验结果汇总
- `report.pdf`

---

### 成员 2：分类模型负责人（数据处理、建模、实验对比）

#### 主要职责

- 负责训练集和验证集的数据读取与预处理
- 实现 baseline 分类模型
- 对比不同分类算法效果
- 在 `val.csv` 上输出分类结果和准确率
- 保存最终模型供集成使用

#### 具体任务

1. 读取 `train.csv` 和 `val.csv`
2. 分析数据基本情况：
   - 缺失值
   - 重复值
   - 标签分布
3. 实现文本预处理流程，例如：
   - 小写化
   - 去除 URL
   - 去除特殊符号
   - 视情况保留或处理 hashtag / mention
4. 完成 baseline：
   - `TF-IDF + Logistic Regression`
5. 完成模型对比：
   - `TF-IDF + SVM`
   - `TF-IDF + Naive Bayes`
6. 如时间允许，尝试更强模型
7. 输出评价指标：
   - Accuracy
   - Precision / Recall / F1（可选补充）
8. 保存验证集预测结果和模型文件

#### 预期产出

- 数据处理脚本或 notebook
- baseline 训练脚本
- 模型效果对比表
- `val.csv` 预测结果
- 最终分类模型文件

---

### 成员 3：解释模块负责人（可解释性设计、案例分析、LLM 接入）

#### 主要职责

- 设计判断依据输出逻辑
- 实现规则化解释模块
- 尝试接入学校提供的大语言模型接口
- 整理典型案例用于报告展示
- 支持可解释性部分撰写

#### 具体任务

1. 设计规则化解释方案，例如从以下角度生成依据：
   - 是否存在夸张或煽动性表达
   - 是否使用绝对化词语
   - 是否缺少可靠来源
   - 是否接近常见谣言表达模式
2. 结合分类模型输出，生成简洁、具体的解释文本
3. 如条件允许，调用学校提供的大语言模型接口生成更自然的判断依据
4. 整理 5–10 个典型案例，包括：
   - 输入文本
   - 真实标签
   - 预测结果
   - 判断依据
   - 简要分析
5. 为报告中的可解释性部分提供内容支持

#### 预期产出

- `explain.py`
- 解释模板或 prompt 方案
- 可解释性案例文件
- 报告中的案例分析素材

---

## 五、协作规范

为确保最终仓库能够体现三位成员的真实贡献，建议采用以下协作规范：

1. 每位成员都必须直接向仓库提交代码或文档
2. 提交信息需清晰说明修改内容，例如：
   - `feat: add tfidf logistic baseline`
   - `feat: add explanation module`
   - `docs: update README and report`
3. 每位成员尽量在自己负责的模块中集中提交
4. 每次提交前先本地运行，确保代码基本可用
5. 所有核心结果文件、图表和案例统一放在约定目录中

---

## 六、阶段推进建议

### 第一阶段：完成最小可用版本

目标：先打通完整流程。

任务：

- 完成数据读取
- 跑通 baseline 分类模型
- 实现简单解释模块
- 支持输入文本并输出 `label + reason`

### 第二阶段：优化分类效果

目标：提升 `val.csv` 上的准确率。

任务：

- 改进文本预处理
- 对比多个传统模型
- 必要时尝试预训练模型
- 形成实验对比结果表

### 第三阶段：提升解释质量

目标：让判断依据更自然、更具体。

任务：

- 优化规则解释
- 尝试接入大模型接口
- 整理典型样例

### 第四阶段：完成交付材料

目标：整理最终提交版本。

任务：

- 清理仓库结构
- 补全 `README.md`
- 完成 `report.pdf`
- 检查每位成员贡献记录

---

## 七、最终交付物清单

最终仓库应至少包含以下内容：

- `README.md`
- `report.pdf`
- 训练与预测代码
- 解释模块代码
- 支持运行的依赖文件
- 结果输出文件或案例文件

README 中至少应说明：

- 项目简介
- 环境依赖
- 安装方法
- 数据准备方式
- 训练命令
- 推理命令
- 输出格式说明
- 项目结构说明

---

## 八、组内简要执行目标

本项目最终要实现的最基本能力是：

> 输入一段英文社交媒体文本，系统输出该文本是否为谣言，以及一段能够说明判断原因的自然语言依据。

在此基础上，项目还需兼顾：

- 分类准确率
- 解释质量
- 可运行性
- 报告完整性
- 分工可追踪性
